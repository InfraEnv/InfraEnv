export function renderRuntimeModule(catalog) {
  const serialized = JSON.stringify(catalog, null, 2);
  return `/* SPDX-License-Identifier: CC-BY-4.0 */\n/* DO NOT EDIT — generated educational data from vendor/curriculum/catalog.json by content:sync. */\nimport type { LabDefinition, RuntimeCurriculumProfile } from "@infraenv/shared";\n\nexport const runtimeCurriculumProfile = ${serialized} as unknown as RuntimeCurriculumProfile;\n\nfunction required<T>(value: T | undefined, id: string): T {\n  if (!value) throw new Error(\`Generated curriculum snapshot is missing \${id}.\`);\n  return value;\n}\n\nexport const findSlowWorkerScenarioDocument: Record<string, unknown> = required(\n  runtimeCurriculumProfile.scenarios.find((item) => item.id === "scenario:slow-worker-bandwidth-drop" && item.version === "2.0.0"),\n  "scenario:slow-worker-bandwidth-drop@2.0.0"\n);\n\nexport const findSlowWorkerLab: LabDefinition = required(\n  runtimeCurriculumProfile.labs.find((item) => item.id === "lab:find-slow-worker"),\n  "lab:find-slow-worker"\n);\n`;
}
